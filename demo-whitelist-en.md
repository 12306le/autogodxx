# API 白名单（英文方法名 / 来自示例）

> 下列英文方法名全部来自上传 zip 的官方示例代码。
> **编写英文 API 调用时，优先以此文件和对应 demo 文件为准。**
> 如果某个英文方法名不在这里出现，不要自行把中文方法名翻译成英文。

## $act

- 示例中出现过的方法：
  - `$act.activity()`
  - `$act.back()`
  - `$act.click()`
  - `$act.gesture()`
  - `$act.getPermit()`
  - `$act.hasPermit()`
  - `$act.home()`
  - `$act.input()`
  - `$act.lock()`
  - `$act.menu()`
  - `$act.move()`
  - `$act.notifications()`
  - `$act.power()`
  - `$act.press()`
  - `$act.screenshot()`
  - `$act.settings()`
  - `$act.splitScreen()`
  - `$act.unlock()`

- 相关示例文件：`demo-act.md`


## $alert

- 示例中出现过的方法：
  - `$alert.create()`

- 相关示例文件：`demo-alert.md`


## $app

- 示例中出现过的方法：
  - `$app.appName()`
  - `$app.appSetting()`
  - `$app.call()`
  - `$app.editFile()`
  - `$app.getIcon()`
  - `$app.getUsagePermit()`
  - `$app.hasUsagePermit()`
  - `$app.ls()`
  - `$app.lsRecent()`
  - `$app.lsUserApp()`
  - `$app.newIntent()`
  - `$app.openUrl()`
  - `$app.pkgExists()`
  - `$app.pkgName()`
  - `$app.run()`
  - `$app.runApp()`
  - `$app.runPkg()`
  - `$app.sendBroadcast()`
  - `$app.sendMail()`
  - `$app.sendSms()`
  - `$app.shareImg()`
  - `$app.shareText()`
  - `$app.startActivity()`
  - `$app.uninstall()`
  - `$app.uninstallApp()`
  - `$app.uninstallPkg()`
  - `$app.viewFile()`

- 相关示例文件：`demo-app.md`


## $arc

- 示例中出现过的方法：
  - `$arc.body()`
  - `$arc.close()`
  - `$arc.closeAll()`
  - `$arc.has()`
  - `$arc.item()`

- 相关示例文件：`demo-arc.md`


## $bus

- 示例中出现过的方法：
  - `$bus.clear()`
  - `$bus.close()`
  - `$bus.event()`
  - `$bus.ls()`
  - `$bus.post()`
  - `$bus.stopAll()`

- 相关示例文件：`demo-bus.md`


## $canvas

- 示例中出现过的方法：
  - `$canvas.create()`
  - `$canvas.paint()`

- 相关示例文件：`demo-canvas.md`


## $color

- 示例中出现过的方法：
  - `$color.a()`
  - `$color.argb()`
  - `$color.b()`
  - `$color.equals()`
  - `$color.find()`
  - `$color.findAll()`
  - `$color.g()`
  - `$color.parse()`
  - `$color.r()`
  - `$color.rgb()`
  - `$color.similar()`
  - `$color.str()`
  - `$color.toString()`

- 相关示例文件：`demo-color.md`


## $crypt

- 示例中出现过的方法：
  - `$crypt.aesJiami()`
  - `$crypt.aesJiemi()`
  - `$crypt.aesKey()`
  - `$crypt.digest()`
  - `$crypt.digests()`
  - `$crypt.generateKeyPair()`
  - `$crypt.jiami()`
  - `$crypt.jiemi()`
  - `$crypt.md516()`
  - `$crypt.md532()`
  - `$crypt.md5Key()`
  - `$crypt.sign()`
  - `$crypt.verify()`

- 相关示例文件：`demo-crypt.md`


## $date

- 示例中出现过的方法：
  - `$date.d()`
  - `$date.date()`
  - `$date.dt()`
  - `$date.dur()`
  - `$date.format()`
  - `$date.info()`
  - `$date.parse()`

- 相关示例文件：`demo-date.md`


## $device

- 示例中出现过的方法：
  - `$device.getAlarmMaxVolume()`
  - `$device.getAlarmVolume()`
  - `$device.getAndroidId()`
  - `$device.getAvailMem()`
  - `$device.getBattery()`
  - `$device.getBrightness()`
  - `$device.getBrightnessMode()`
  - `$device.getIMEI()`
  - `$device.getMacAddress()`
  - `$device.getMusicMaxVolume()`
  - `$device.getMusicVolume()`
  - `$device.getNotificationMaxVolume()`
  - `$device.getNotificationVolume()`
  - `$device.getTotalMem()`
  - `$device.isCharging()`
  - `$device.isLock()`
  - `$device.isScreenOff()`
  - `$device.isScreenOn()`

- 相关示例文件：`demo-device.md`


## $dialog

- 示例中出现过的方法：
  - `$dialog.create()`

- 相关示例文件：`demo-dialog.md`


## $draw

- 示例中出现过的方法：
  - `$draw.circle()`
  - `$draw.clear()`
  - `$draw.closeAll()`
  - `$draw.closeLog()`
  - `$draw.cross()`
  - `$draw.d()`
  - `$draw.dot()`
  - `$draw.e()`
  - `$draw.i()`
  - `$draw.line()`
  - `$draw.log()`
  - `$draw.path()`
  - `$draw.rect()`
  - `$draw.text()`
  - `$draw.v()`
  - `$draw.w()`

- 相关示例文件：`demo-draw.md`


## $engine

- 示例中出现过的方法：
  - `$engine.lsTask()`
  - `$engine.pause()`
  - `$engine.run()`
  - `$engine.runCode()`
  - `$engine.start()`
  - `$engine.stopAll()`

- 相关示例文件：`demo-engine.md`


## $event

- 示例中出现过的方法：
  - `$event.on()`

- 相关示例文件：`demo-event.md`


## $excel

- 示例中出现过的方法：
  - `$excel.loop()`
  - `$excel.ls()`
  - `$excel.num()`
  - `$excel.read()`
  - `$excel.write()`

- 相关示例文件：`demo-excel.md`


## $ext

- 示例中出现过的方法：
  - `$ext.loadApk()`
  - `$ext.loadDex()`
  - `$ext.loadDexs()`
  - `$ext.loadJar()`
  - `$ext.loadJars()`

- 相关示例文件：`demo-ext.md`


## $fc

- 示例中出现过的方法：
  - `$fc.show()`

- 相关示例文件：`demo-fc.md`


## $file

- 示例中出现过的方法：
  - `$file.append()`
  - `$file.copy()`
  - `$file.create()`
  - `$file.del()`
  - `$file.exists()`
  - `$file.isDir()`
  - `$file.isEmpty()`
  - `$file.isEmptyDir()`
  - `$file.isFile()`
  - `$file.join()`
  - `$file.ls()`
  - `$file.mkdir()`
  - `$file.move()`
  - `$file.name()`
  - `$file.read()`
  - `$file.rename()`
  - `$file.write()`

- 相关示例文件：`demo-file.md`


## $floaty

- 示例中出现过的方法：
  - `$floaty.closeAll()`
  - `$floaty.getPermit()`
  - `$floaty.newAdj()`
  - `$floaty.newApp()`
  - `$floaty.newSelect()`
  - `$floaty.newSys()`

- 相关示例文件：`demo-floaty.md`


## $http

- 示例中出现过的方法：
  - `$http.download()`
  - `$http.post()`

- 相关示例文件：`demo-http.md`


## $img

- 示例中出现过的方法：
  - `$img.findMultiColors()`
  - `$img.read()`
  - `$img.show()`
  - `$img.threshold()`

- 相关示例文件：`demo-img.md`


## $log

- 示例中出现过的方法：
  - `$log.d()`
  - `$log.e()`
  - `$log.floaty()`
  - `$log.i()`
  - `$log.log()`
  - `$log.v()`
  - `$log.w()`

- 相关示例文件：`demo-log.md`


## $media

- 示例中出现过的方法：
  - `$media.play()`

- 相关示例文件：`demo-media.md`


## $ocr

- 示例中出现过的方法：
  - `$ocr.detect()`
  - `$ocr.detectPath()`
  - `$ocr.init()`
  - `$ocr.v()`

- 相关示例文件：`demo-ocr.md`


## $permit

- 示例中出现过的方法：
  - `$permit.call()`
  - `$permit.camera()`
  - `$permit.floaty()`
  - `$permit.getPermit()`
  - `$permit.hasCall()`
  - `$permit.hasCamera()`
  - `$permit.hasFloaty()`
  - `$permit.hasLoc()`
  - `$permit.hasNet()`
  - `$permit.hasNotifyAccess()`
  - `$permit.hasPermit()`
  - `$permit.hasReadCalendar()`
  - `$permit.hasReadContact()`
  - `$permit.hasReadSms()`
  - `$permit.hasRecord()`
  - `$permit.hasSd()`
  - `$permit.hasSendSms()`
  - `$permit.hasSet()`
  - `$permit.hasWriteCalendar()`
  - `$permit.hasWriteContact()`
  - `$permit.hasWza()`
  - `$permit.loc()`
  - `$permit.net()`
  - `$permit.notifyAccess()`
  - `$permit.readCalendar()`
  - `$permit.readContact()`
  - `$permit.readSms()`
  - `$permit.record()`
  - `$permit.sd()`
  - `$permit.sendSms()`
  - `$permit.set()`
  - `$permit.writeCalendar()`
  - `$permit.writeContact()`
  - `$permit.wza()`

- 相关示例文件：`demo-permit.md`


## $qr

- 示例中出现过的方法：
  - `$qr.make()`
  - `$qr.parse()`

- 相关示例文件：`demo-qr.md`


## $res

- 示例中出现过的方法：
  - `$res.create()`

- 相关示例文件：`demo-res.md`


## $root

- 示例中出现过的方法：
  - `$root.lock()`
  - `$root.unlock()`


## $screen

- 示例中出现过的方法：
  - `$screen.bright()`
  - `$screen.dir()`
  - `$screen.getDensity()`
  - `$screen.getHeight()`
  - `$screen.getPermit()`
  - `$screen.getScreen()`
  - `$screen.getScreenInfo()`
  - `$screen.getWidth()`
  - `$screen.hasPermit()`
  - `$screen.isScreenOff()`
  - `$screen.isScreenOn()`
  - `$screen.save()`

- 相关示例文件：`demo-screen.md`


## $sqlite

- 示例中出现过的方法：
  - `$sqlite.add()`
  - `$sqlite.close()`
  - `$sqlite.delDb()`
  - `$sqlite.delTab()`
  - `$sqlite.exe()`
  - `$sqlite.ls()`
  - `$sqlite.newTab()`
  - `$sqlite.open()`
  - `$sqlite.update()`

- 相关示例文件：`demo-sqlite.md`


## $storage

- 示例中出现过的方法：
  - `$storage.create()`


## $str

- 示例中出现过的方法：
  - `$str.isEmpty()`
  - `$str.random()`
  - `$str.startWith()`
  - `$str.subEnd()`
  - `$str.subStart()`
  - `$str.trim()`

- 相关示例文件：`demo-str.md`


## $sys

- 示例中出现过的方法：
  - `$sys.airplane()`
  - `$sys.clearClip()`
  - `$sys.enableCall()`
  - `$sys.getCallPermit()`
  - `$sys.getClip()`
  - `$sys.getDeviceId()`
  - `$sys.getSetPermit()`
  - `$sys.hasClip()`
  - `$sys.ignorePower()`
  - `$sys.requestIgnorePower()`
  - `$sys.setClip()`
  - `$sys.volume()`

- 相关示例文件：`demo-sys.md`


## $szk

- 示例中出现过的方法：
  - `$szk.exe()`
  - `$szk.hasPermit()`


## $task

- 示例中出现过的方法：
  - `$task.getPath()`
  - `$task.isRunning()`
  - `$task.pause()`
  - `$task.start()`
  - `$task.stop()`

- 相关示例文件：`demo-task.md`


## $thread

- 示例中出现过的方法：
  - `$thread.create()`
  - `$thread.loop()`
  - `$thread.run()`
  - `$thread.stopAllLoop()`
  - `$thread.stopLoop()`

- 相关示例文件：`demo-thread.md`


## $tip

- 示例中出现过的方法：
  - `$tip.calendar()`
  - `$tip.clock()`
  - `$tip.d()`
  - `$tip.date()`
  - `$tip.e()`
  - `$tip.i()`
  - `$tip.input()`
  - `$tip.more()`
  - `$tip.one()`
  - `$tip.show()`
  - `$tip.time()`
  - `$tip.v()`
  - `$tip.w()`

- 相关示例文件：`demo-tip.md`


## $touch

- 示例中出现过的方法：
  - `$touch.click()`
  - `$touch.hasPermit()`
  - `$touch.init()`
  - `$touch.move()`
  - `$touch.press()`

- 相关示例文件：`demo-touch.md`


## $tts

- 示例中出现过的方法：
  - `$tts.read()`

- 相关示例文件：`demo-tts.md`


## $ui

- 示例中出现过的方法：
  - `$ui.layout()`

- 相关示例文件：`demo-ui.md`


## $ws

- 示例中出现过的方法：
  - `$ws.create()`

- 相关示例文件：`demo-ws.md`
